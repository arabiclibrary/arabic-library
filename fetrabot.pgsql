fetrabot/
├── fetrabot.py           ← ملف تشغيل البوت
├── principles.json       ← المبادئ الفطرية
├── memory.json           ← المحادثات والملاحظات
├── requirements.txt      ← لتحديد المكتبات المطلوبة
└── .env                  ← لتخزين المعلومات الحساسة مثل API keys
